<h1>Listing breweries</h1>

<table>
  <thead>
  <tr>
    <th> <%= link_to 'Name', breweries_path(order:"name") %> </th>
    <th> <%= link_to 'Year', breweries_path(order:"year") %> </th>
  </tr>
  </thead>

  <tbody>
  <% @breweries.each do |brewery| %>
      <tr>
        <td><%= link_to brewery.name, brewery.name %></td>
        <td><%= brewery.year %></td>
      </tr>
  <% end %>
  </tbody>
</table>
    <p> Number of breweries: <%= @breweries.count %> </p>

<p> <%=link_to 'List of breweries', breweries_path%> </p>

<%= link_to 'New Brewery', new_brewery_path %>
